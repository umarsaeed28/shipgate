{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm db:generate && pnpm --filter @qa/app run build",
  "env": {
    "CLIENT_SLUG": "acme",
    "APP_BASE_PATH": "/app-acme"
  }
}
