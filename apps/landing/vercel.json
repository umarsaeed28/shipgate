{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "(cd ../.. && pnpm db:generate) && next build",
  "env": {
    "CLIENT_SLUG": "acme"
  }
}
