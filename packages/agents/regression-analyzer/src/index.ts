// Agent 2 — Regression analyzer. Ingests Jenkins runs and classifies failures
// as real_bug / test_issue / flaky. It classifies and explains only; it does
// NOT edit tests or mask failures. SCAFFOLD ONLY (Step 5).
export {};
