// Agent 3 — Trend analyzer. Charts over the history store + a chat endpoint that
// answers ONLY from rows it queried (never a number it did not retrieve).
// Read-only over the store. SCAFFOLD ONLY (Step 5).
export {};
