// Zod schemas for structured agent output (scenario, classification, decision).
// Built in Step 3. Agents must validate Claude output against these and retry
// on invalid output (see qa-platform.mdc).
export {};
