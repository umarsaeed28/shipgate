// Claude client: complete() and completeStructured(schema). ALL Claude calls go
// through this package; model comes from env (ANTHROPIC_MODEL), prompts are
// versioned templates. Built in Step 3 (see qa-platform.mdc).
export {};
